require 'spec_helper'

describe NewRelicRMQPlugin do
  it 'has a version number' do
    expect(NewRelicRMQPlugin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
